# Tests if multiple nodes can be added to the graph

# Create a graph
CreateGraph multiNodeGraph

# Add nodes
AddNode multiNodeGraph a
AddNode multiNodeGraph b

# Print the nodes in the graph and the outgoing edges from a and b
ListNode multiNodeGraph
ListChildren multiNodeGraph a
ListChildren multiNodeGraph b